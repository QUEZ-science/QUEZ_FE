import React from 'react';
import { BrowserRouter, Routes, Router, Navigate } from 'react-router-dom';

const App: React.FC = ()=>{
  return(
    <BrowserRouter>
    </BrowserRouter>
  )
}

export default App;