import * as _ from '@/styles/Home';
import Header from '@/components/Header';

const Home = () => {
  return (
    <_.Container>
      <Header />
      <_.Main></_.Main>
    </_.Container>
  );
};

export default Home;
